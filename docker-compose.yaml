version: "3"
services:
  web:
    # image: 'name_of_image'
    build: .
    command: "node index.js"
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
    depends_on:
      - "mongo"
  mongo:
    image: "mongo"
    ports:
      - "27017:27017"
